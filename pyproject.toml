[project]
name = "my-hello-agent"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
my-hello-agent = "my_hello_agent:main"

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
